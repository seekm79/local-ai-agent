# User projects live here — never commit them.
/workspace/

# Python
backend/.venv/
backend/venv/
__pycache__/
*.pyc
.pytest_cache/

# SQLite
backend/*.db
backend/*.db-wal
backend/*.db-shm

# Node
frontend/node_modules/
frontend/dist/
frontend/*.tsbuildinfo
# tsc -b emit artifacts (config is bundler-only; Vite handles the real build)
frontend/vite.config.js
frontend/vite.config.d.ts

# Editor / OS
.DS_Store
Thumbs.db
.idea/
*.log

# Packaged build-kit artifact (redundant zip of templates/)
workbench-build-kit.zip
