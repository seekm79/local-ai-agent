# Pinned-ish runtime deps for the venv. `pip install -r requirements.txt`.
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
openai>=1.40
aiosqlite>=0.20
python-multipart>=0.0.9
websockets>=12.0
pywinpty>=2.0; sys_platform == "win32"
playwright>=1.40
pillow>=10.0
pypdf>=4.0

# dev / tests
pytest>=8.0
pytest-asyncio>=0.23
